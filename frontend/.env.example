# ════════════════════════════════════════════════════════════════════════════
#  前端环境变量
#
#  这里**只需要一个**配置项。所有模型 Key 都放在 backend/.env：
#  写在 `VITE_` 前缀下会被打包进浏览器产物，等于把密钥公开发布。
#  前端的模型调用全部经后端（`/agent` 代理）转发，前端不接触任何密钥。
# ════════════════════════════════════════════════════════════════════════════

# ── 多智能体后端（FastAPI + LangGraph）──────────────────────────────────────
# dev server 与生产 server.mjs 都会把 /agent 代理到这里。
# 复原任务与领域问答（讲解助手）都走这一条。
AGENT_BACKEND_URL=http://127.0.0.1:8123

# ── 已移除的配置项（不要加回来）──────────────────────────────────────────────
# CHAT_APP_API_KEY / VITE_CHAT_APP_API_KEY
#   讲解助手原先转发给外部 Dify 服务，只拿回一段没有出处的文字。
#   现已改调本项目的 POST /api/ask：答案与引用一起返回，
#   引用里的出处、原文、链接全部取自语料条目。
#
# VITE_DASHSCOPE_API_KEY
#   从未被任何代码使用，declaration 也已从 vite-env.d.ts 移除。
#   留着只会诱使人误填 —— 一旦填了就会被打包进前端产物。
